<UserControl x:Class="MyIPTV.App.Views.GuideView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MyIPTV.App.Controls">
    <Grid Margin="32,24,32,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock AutomationProperties.HeadingLevel="Level1" FontSize="30" FontWeight="SemiBold" Text="{Binding Title}" />
            <TextBlock Margin="0,6,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="15" Text="{Binding Subtitle}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,18,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBox AutomationProperties.Name="XMLTV source" Style="{StaticResource FormTextBoxStyle}"
                     Text="{Binding Source, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Margin="10,0,0,0" AutomationProperties.Name="Refresh XMLTV guide"
                    Command="{Binding RefreshGuideCommand}" Content="Refresh guide" Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>

        <Border Grid.Row="2" Margin="0,14,0,0" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding StatusMessage}" />
                    <ListBox Grid.Row="1" Margin="0,12,0,0" AutomationProperties.Name="TV guide channels"
                             ItemsSource="{Binding Rows}" ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                             Style="{StaticResource NavigationListBoxStyle}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource NavigationItemStyle}">
                                <Setter Property="AutomationProperties.Name" Value="{Binding ChannelName}" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4,5">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="170" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                    <TextBlock Margin="8" FontSize="16" FontWeight="SemiBold" Text="{Binding ChannelName}" TextWrapping="Wrap" />
                                    <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                        <ItemsControl ItemsSource="{Binding Programs}">
                                            <ItemsControl.ItemContainerStyle>
                                                <Style TargetType="ContentPresenter">
                                                    <Setter Property="AutomationProperties.Name" Value="{Binding Title}" />
                                                </Style>
                                            </ItemsControl.ItemContainerStyle>
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="210" MinHeight="76" Margin="0,0,8,0" Padding="10"
                                                            Background="{DynamicResource SurfaceElevatedBrush}" CornerRadius="8">
                                                        <StackPanel>
                                                            <TextBlock FontWeight="SemiBold" Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                                                            <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource AccentBrush}" FontSize="11" Text="{Binding TimeLabel}" />
                                                            <TextBlock Margin="0,3,0,0" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"
                                                                       Text="{Binding Description}" TextTrimming="CharacterEllipsis" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Grid.Style>
                        <Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers><DataTrigger Binding="{Binding HasRows}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                        </Style>
                    </Grid.Style>
                </Grid>
                <controls:StatePanel AutomationProperties.Name="No guide data" Glyph="{Binding EmptyGlyph}"
                                     Message="{Binding EmptyMessage}" State="{Binding State}" Title="{Binding EmptyTitle}">
                    <controls:StatePanel.Style>
                        <Style TargetType="controls:StatePanel"><Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasRows}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger>
                                <DataTrigger Binding="{Binding IsBusy}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </controls:StatePanel.Style>
                </controls:StatePanel>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Loading guide…"
                           Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
