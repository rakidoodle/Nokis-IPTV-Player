<UserControl x:Class="MyIPTV.App.Views.SectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MyIPTV.App.Controls">
    <Grid Margin="32,28,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock
                AutomationProperties.HeadingLevel="Level1"
                FontSize="30"
                FontWeight="SemiBold"
                Text="{Binding Title}" />
            <TextBlock
                Margin="0,7,0,0"
                Foreground="{DynamicResource TextSecondaryBrush}"
                FontSize="15"
                Text="{Binding Subtitle}"
                TextWrapping="Wrap" />
        </StackPanel>

        <Border
            Grid.Row="1"
            Margin="0,24,0,0"
            Style="{StaticResource CardBorderStyle}">
            <controls:StatePanel
                Glyph="{Binding EmptyGlyph}"
                Message="{Binding EmptyMessage}"
                State="{Binding State}"
                Title="{Binding EmptyTitle}" />
        </Border>
    </Grid>
</UserControl>
